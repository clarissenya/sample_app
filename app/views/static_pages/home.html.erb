<% if logged_in? %>
  <div class="row">
    <div class="col-md-4">
      <aside class="user">
        <section class="user_info">
          <%= render 'shared/user_info' %>
        </section>
        <section class="stats">
          <%= render 'shared/stats' %>
        </section>
      </aside>
      <div>
      <%= render 'shared/recommend' %>
      </div>
    </div>
    
    <div class="col-md-8">
      <div class="home_title"><h3>Mypage</h3></div>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <h1>Welcome to the Sample App</h1>

    <h2>
      This is the home page for the sample application.
    </h2>

    <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>